# ===========================================
# 📘 IronCoach Environment Example
# ===========================================
# 이 파일은 환경 변수 예시 템플릿입니다.
# 실제 키나 비밀번호를 포함하지 않으며, 복제하여 `.env`로 사용하세요.
# 
# 사용법:
# 1️⃣ 이 파일을 복사하여 `.env` 로 이름 변경
# 2️⃣ 각 항목을 자신의 환경에 맞게 수정
# ===========================================

# === FastAPI 설정 ===
APP_NAME=IronCoach
APP_ENV=development
APP_PORT=8000

# === Database 설정 ===
# 로컬 개발 또는 테스트 시 SQLite 사용 예시
DATABASE_URL=sqlite:///./ironcoach.db

# === OpenAI API Key ===
# 본인 계정의 키로 교체 필요 (예시용)
OPENAI_API_KEY=sk-yourapikeyhere

# === Vector DB (RAG용 Chroma) ===
# 문서 임베딩이 저장될 경로
VECTOR_DB_PATH=./backend/vectorstore

# === Environment 정보 ===
ENVIRONMENT=development
